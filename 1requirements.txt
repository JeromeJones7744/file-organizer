# File Organizer — Dependencies
# All core features require only the Python standard library (3.8+).
# The packages below are optional — install only what you need.

# Watch mode (--watch): auto-organize files as they are added to the folder
watchdog>=4.0.0

# Progress bar (--progress): shows a live progress indicator during organize
tqdm>=4.60.0

# GUI (--gui): graphical interface — usually bundled with Python already
# If missing on Linux: sudo apt install python3-tk
# tkinter is part of the stdlib; no pip install needed

# Testing
pytest>=7.0.0
